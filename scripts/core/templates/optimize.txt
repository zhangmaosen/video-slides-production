【第 ${NEXT}/${ITERATIONS} 轮：优化 slide_${SLIDE_FMT} prompt】

当前最高分：v${BEST_VERSION} = ${BEST_SCORE}分

## 第一步：读取必要文件

1. 当前最高分 prompt：${PROMPT_DIR}/v${BEST_VERSION}_positive.txt
2. 逐字稿：${PROJECT_DIR}/slides_content.json 中 slide_${SLIDE_FMT}
   - 核对这页该有什么文字
3. 如果二郎神反馈涉及参考对象：
   - 读取 ${PROJECT_DIR}/assets/ref_meta.json 确认参考要素
   - 读取对应参考图分析特征差异

## 第二步：分析二郎神反馈

${SCORE_FEEDBACK}

重点关注：
- [关键问题] 列出的 2-3 个问题
- [给女娲的修改指令] 里的具体操作

## 第三步：针对性修改

修改原则：
- **只改有问题的部分**，不动本来正确的描述
- 保持总字符数 400-700，不超过 800
- 文字被扣分 → 简化文字，缩短字数，用数字代替中文
- 场景被扣分 → 调整场景描述
- 参考对象被扣分 → 读参考图，修正特征描述
- AI 瑕疵被扣分 → 在负向提示词追加具体排除项（如 deformed fingers）

## 输出
写入：
- ${PROMPT_DIR}/v${NEXT}_positive.txt
- ${PROMPT_DIR}/v${NEXT}_negative.txt

回复：字符数 + 改了什么（一句话）
